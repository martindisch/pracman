version: '3.7'
services:
  db:
    image: mongo:4-bionic
    restart: always
    volumes:
      - mongo:/data/db
  app:
    build: app
    env_file:
      - .env
    restart: always
    depends_on:
      - db
    ports:
      - '8080:3000'
volumes:
  mongo:
